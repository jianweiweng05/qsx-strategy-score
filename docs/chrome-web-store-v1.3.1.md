# Chrome Web Store submission - 1.3.1

## Upload package

- File: `dist/qsx-strategy-score-chrome-v1.3.1.zip`
- SHA-256: `88cd23ed3dbcdf36953c925bb8a650eeda17a1132de82ab329b9bc1303bd4ad0`
- Source commit: `a65d0c71da4cf486942ca93325e458b75a23e198`

## What's fixed

Version 1.3.1 consumes the shared scorer service's localized dynamic diagnostic fields. The selected language now applies to autocorrelation, implausibly high Sharpe, suspicious smoothness, CAGR, Calmar, growth, and other risk flags in both the compact result panel and expanded details.

The public scorer is `qsx-strategy-score v0.3.4`; the private service pins that tag and returns stable English `msg` fields alongside `msg_local`, `problem_local`, and `direction_local`. PDF layout remains generated by the same hosted `v0.3.3`/`v0.3.4` CJK-safe core path.

## Verification

- Public core tests: `54 passed`.
- Chrome locale/version checks and Node syntax checks passed.
- ZIP integrity check passed; manifest version is `1.3.1`.
- Live `/api/score` smoke passed for `zh`, `ja`, `ko`, `es`, and `pt-BR` using `sample_flagged.csv`; each returned localized dynamic flag text.
